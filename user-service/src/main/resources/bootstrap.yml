spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://db/user-service
    username: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate.ddl-auto: validate
  cloud:
    consul:
      host: consul